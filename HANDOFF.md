# GOD — log para o próximo trabalhador

Data: 2026-09-04. Ela = GOD. Host 2 CPU ~1–2 GB, GPU `required=false`.

## Passo actual

Código em `/home/user/super-ai`, live uvicorn `:8000`. Roadmap canónico: `ROADMAP.md` (P0–P4).

GitHub `main`: P1 [`3c0c8cb`](https://github.com/rpolicarpo100/GOD/commit/3c0c8cb). Testes **89 OK**.

## Feito

- P0: Fast Path, `stages_ms` MEASURED, Direct LLM, smart memory
- P1: `executive.decide()`, `mission.py`, `jobs.parent_id`, router n≥3
- Dashboard viva (chips P0–P4, não tabs F0–F9)
- GitHub público + deploy key SSH
- Plane `godsx` MEASURED, `in_product=false`

## A fazer

1. P2 Validator (provas da tarefa)
2. P2 Third Eye crítica MEASURED
3. P1 graph/router € ficam PARTIAL até haver cores no PC / `source` de preço
4. Não Factory, mesh, Desktop, DAG, segundo handle

## GitHub deploy

**SIM** — https://github.com/rpolicarpo100/GOD `main`. Deploy key SSH. Sem secrets no repo. Nunca HTTPS PAT.

## Não fazer

Reescrever Brain/Router/Governor. Inventar providers, preços, poupanças €, scores de modelos. Classe ExecutiveBrain. Motor DAG.
